# Sandbox-local pytest config for the edge unit test. Isolated from core (core
# runs from the repo root with its own pyproject testpaths=["tests"], so it never
# collects this). asyncio_mode=auto lets the async tests + fixtures run without
# per-test decorators.
[pytest]
asyncio_mode = auto
