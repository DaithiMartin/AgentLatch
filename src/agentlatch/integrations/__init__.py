"""Optional, framework-specific integrations for AgentLatch.

Each integration may require an extra (e.g. ``pip install agentlatch[fastapi]``)
and is import-guarded so the core library never depends on a web framework.
"""
